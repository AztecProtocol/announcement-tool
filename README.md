# aztec-announce

Release-only announcement pipeline (author once → fan out). Concept doc: deliverable-8-announcement-tool/announcement-tool-concept.md

Dev: `docker compose -f docker-compose.dev.yml up -d` then `npm run migrate && npm test`.
