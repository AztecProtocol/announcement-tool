# Rendered by Terraform (vm.tf) — do not edit by hand.
# The host is addressed by tailnet MagicDNS name; run the playbook from a
# machine on the tailnet.
all:
  hosts:
    ${announce_host_name}:
      ansible_user: root
      data_mount: /mnt/HC_Volume_${data_volume_id}
